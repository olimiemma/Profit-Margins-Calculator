def calculate_total(number):
  """Calculates the total after applying the given steps.

  Args:
    number: The initial number.

  Returns:
    The final total.
  """

  # Multiply by 35% and add the result to the initial number.
  total = number * (1 + 0.35)

  # Multiply by 18% and add the result to the previous total.
  total = total * (1 + 0.18)

  return total


# Get the initial number from the user.
initial_number = float(input("Enter the initial number: "))

# Calculate the total.
total = calculate_total(initial_number)

# Print the total.
print("The final total is:", total)
